const profileTypeClient = 'client';
const profileTypeContractor = 'contractor';

const profileTypes = {
  [profileTypeClient]: profileTypeClient,
  [profileTypeContractor]: profileTypeContractor,
};

module.exports = {
  profileTypeClient,
  profileTypeContractor,
  profileTypes,
};
